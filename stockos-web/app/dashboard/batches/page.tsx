'use client';

import Week3DeferredPage from '@/components/dashboard/Week3DeferredPage';

export default function BatchesPage() {
  return (
    <Week3DeferredPage
      title="Batches & expiry"
      description="Batch tracking lands in Week 3+ when batch tables ship."
    />
  );
}
